const checkValidation = (name) => {
    const tempName = name.trim();
    if (!tempName || tempName === "") return "Task is empty"
    // if (!desc) return "Description is empty"
}

export default checkValidation;
